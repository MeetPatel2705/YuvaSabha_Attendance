node_modules/
.env
server/data/attendance.sqlite
server/data/attendance-live.xlsx
server/data/backups/
server/tests/*.sqlite
client/dist/
*.log

# Real community roster (names + mobile numbers) — never committed to a
# public repo. See server/scripts/members.seed.example.json and
# server/data/Yuva Sabha Harinagar.example.xlsx for sanitized stand-ins with
# the same structure, used for local setup/testing instead.
server/data/Yuva Sabha Harinagar.xlsx
server/scripts/members.seed.json
